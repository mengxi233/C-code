#include <stdio.h>
int main ()
{
  
  /* int i,n,a=0,b=0,c=0;
    char ch;
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        ch = getchar();
        if (ch>='A'&&ch<='z')
        a++;
        else if(ch==' '||ch=='\n');
        b++;
    if(ch>=0&&ch<=9)
        c++;

        
    }
    printf("%d %d %d",a,b,c);
  */
   printf(" ");
    return 0 ;
    
   
}
